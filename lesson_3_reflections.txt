When would you want to use a remote repository rather than keeping all your work
local?
-When collaborating. Or for storage.

Describe the differences between forks, clones and branches. When would you use
one instead of another?
-Fork: essentially a copy of a full repo
-Clone: a copy of a repository. all forks are clones, not all clones are forks
-Branches: a path in a repository where versions of a file have deviated.
